{{ if eq "certificate" .Values.api.tls.mode }}
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ template "armada.name" . }}-api
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "armada.labels" . | nindent 4 }}
spec:
  commonName: {{ template "armada.name" . }}.{{ .Release.Namespace }}.svc
  dnsNames:
  - "{{ template "armada.name" . }}.{{ .Release.Namespace }}.svc.cluster.local:{{ .Values.api.port }}"
  - "{{ template "armada.name" . }}.{{ .Release.Namespace }}.svc.cluster.local"
  - "{{ template "armada.name" . }}.{{ .Release.Namespace }}:{{ .Values.api.port }}"
  - "{{ template "armada.name" . }}.{{ .Release.Namespace }}"
  - "{{ template "armada.name" . }}:{{ .Values.api.port }}"
  - "{{ template "armada.name" . }}"
  - "{{ template "armada.name" . }}-api.{{ .Release.Namespace }}.svc.cluster.local:{{ .Values.api.port }}"
  - "{{ template "armada.name" . }}-api.{{ .Release.Namespace }}.svc.cluster.local"
  - "{{ template "armada.name" . }}-api.{{ .Release.Namespace }}:{{ .Values.api.port }}"
  - "{{ template "armada.name" . }}-api.{{ .Release.Namespace }}"
  - "{{ template "armada.name" . }}-api:{{ .Values.api.port }}"
  - "{{ template "armada.name" . }}-api"
{{- if .Values.api.externalLoadBalancer.enabled }}
  - "{{ .Values.api.externalLoadBalancer.externalDomain }}:{{ .Values.api.port }}"
  - {{ .Values.api.externalLoadBalancer.externalDomain }}
{{- end }}
  {{- with .Values.api.tls.issuerRef }}
  issuerRef:
    {{- . | toYaml | nindent 4 }}
  {{- end }}
  secretName: {{ template "armada.name" . }}-api-cert
{{ end }}
